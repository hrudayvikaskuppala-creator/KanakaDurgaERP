customtkinter>=5.2.0
Pillow>=10.0.0
bcrypt>=4.0.0
pygame>=2.5.0

# Document import (Supplier/Product "Import from File" - PDF, Word,
# Excel, CSV, image). CSV needs no extra package (stdlib csv module).
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
pytesseract>=0.3.10
# pytesseract also needs the Tesseract-OCR program itself installed
# on the machine (not a pip package):
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   macOS:   brew install tesseract
#   Linux:   sudo apt install tesseract-ocr

# Barcode / QR generation (product labels, printable tags).
# Optional at runtime: barcode_service.py degrades to a text-only label
# if these aren't installed, but install them for real barcode/QR images.
python-barcode>=0.15.1
qrcode[pil]>=7.4.2

